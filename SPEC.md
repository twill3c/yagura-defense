# SPEC.md — yagura-defense

<!-- scaffold template v1.8.0 から展開(2026-08-13)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

マウス / タッチ操作だけで完結する和風タワーディフェンス。プレイヤーは矢倉(やぐら)を
建てて、街道を進軍する敵勢から本丸を守る。コアは UI から完全に分離された決定論的
シミュレーションであり、シード + コマンド列から結果が完全に再現できる。この性質を使い、
バランス基準(§4 B-xx)をヘッドレスの自動テストとして表明する。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | **決定性**: 乱数はシード付き PRNG(mulberry32)を注入。同一シード + 同一コマンド列(tick 付き)→ 同一の最終 GameState。core で `Date.now()` / `Math.random()` を呼ばない | must |
| F-02 | **盤面**: グリッド(標準 12×9)。タイルは path / buildable / blocked の 3 種。path は湧き口 → 本丸の順序付きセル列(単一経路)。タワー建設は buildable かつ空きセルのみ | must |
| F-03 | **シミュレーション**: 固定論理 tick(100ms)。`step(state): state` は純関数。tick 内の処理順は (1)コマンド適用 (2)湧き (3)敵移動+リーク判定 (4)タワー射撃 (5)撃破処理 (6)勝敗判定 で固定 | must |
| F-04 | **タワー**: 3 種 — 弓矢倉(単体・速射)/ 大筒矢倉(範囲・低速)/ 札矢倉(スロー付与)。射程は塔中心から敵位置へのユークリッド距離。ターゲットは射程内で最も進行(progress)が大きい敵、同値なら若い id | must |
| F-05 | **敵**: 4 種 — 足軽(標準)/ 忍び(高速・低耐久)/ 兜武者(低速・高耐久・防御)/ 武者大将(ボス)。ダメージは `max(1, dmg - def)`。本丸到達でライフ減(通常 1、ボス 5)、敵は消滅 | must |
| F-06 | **ウェーブと経済**: マップがウェーブ定義(敵種・数・間隔・開始遅延)を持つ。ウェーブは `startWave` コマンドでのみ開始(自動開始しない)。撃破報酬 + ウェーブクリアボーナスで資金を得る。ウェーブ進行中も建設可能 | must |
| F-07 | **アップグレード / 売却**: 各タワー Lv1→Lv3。売却の払い戻しは累計投資額の 70%(切り捨て)。ウェーブ進行中も可 | must |
| F-08 | **勝敗とスコア**: 全ウェーブの敵を殲滅で勝利、ライフ 0 で即敗北。スコア = 撃破数×10 + 残ライフ×100 + 残資金。ハイスコアは localStorage(UI 層)に保持 | must |
| F-09 | **ポインタ専用 UI**: 全操作がタップ / クリックのみで完結(ホバー・キーボード・スワイプを必須にしない)。タッチターゲットは 44px 四方以上。建設は「セルをタップ → 種別を選択」の 2 タップ、タワータップで強化/売却パネル | must |
| F-10 | **バランス自動検証**: §4 の B-xx をヘッドレスシミュレーションのテストとして表明し、バランス数値の変更が壊せば verify が落ちる | must |
| F-11 | **マップ生成器**: シードから経路保証付きマップを生成(経路の連結性・自己交差なし・buildable 数下限を検証してから出荷)。生成は試行予算付き | should |
| F-12 | **効果音**: WebAudio 合成(射撃・撃破・リーク・ウェーブ開始)。デフォルト OFF、タップで ON | should |

バランス数値(コスト・HP・射程等)の正本は `src/core/balance.ts` とする。SPEC は数値
そのものではなく制約(B-xx)を規定し、数値調整で SPEC-DRIFT を生まない。

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | Next.js 静的エクスポート。out/ のみで動作しサーバ API を持たない | `next build` 成功 + output: "export" |
| N-02 | `src/core` はカバレッジ lines/functions/statements ≥ 90%, branches ≥ 85% | `vitest run --coverage` の thresholds |
| N-03 | 描画は rAF、シミュレーションは固定 tick の積算で分離(フレームレートが結果に影響しない) | コードレビュー + F-01 の決定性テスト |
| N-04 | ランタイム依存ゼロ追加(next/react/react-dom のみ。devDeps は可) | package.json |

## 4. 品質基準(バランスゲート)

いずれもマップ 1(built-in)に対するヘッドレステストで表明する:

| ID | 基準 |
|---|---|
| B-01 | タワー未設置ならウェーブ 1 で 1 体以上が本丸に到達する(ゲームが自明でない) |
| B-02 | 基準戦略(資金が許す限り推奨マス順に弓矢倉を建てる貪欲スクリプト)で全ウェーブをクリアできる(残ライフ ≥ 1) |
| B-03 | 初期資金で弓矢倉を 2 基以上建設できる |
| B-04 | 建設 → 即売却で資金が増えない(払い戻し < 費用、アップグレード込みでも同様) |
| B-05 | 全敵種に対し全タワー種の 1 発ダメージが ≥ 1(防御で完全無効化されない) |
| B-06 | シード 1〜20 の生成マップ(F-11 実装後)が全数、経路検証と buildable 下限を満たす |

## 5. スコープ外

- キャンペーン進行・マップ間のアンロック管理(マップ選択は単純なリストのみ)
- ウェーブ途中のセーブ / 中断復帰(リロードでリセットされてよい)
- オンラインランキング・アカウント・PWA / オフライン対応
- タワー / 敵の 4 種目以降、特殊能力(飛行・召喚・回復等)
- ゲームパッド・キーボード対応(F-09 の裏面)

## 6. 開発ロードマップ(ループ計画)

| loop | 内容 | 主対応 |
|---|---|---|
| loop_001 | ヘッドレスコアシム(型・PRNG・tick・移動・弓矢倉・湧き・勝敗) | F-01/02/03/06 の基盤 |
| loop_002 | 盤面描画 + タップ建設 + ウェーブ開始(遊べる MVP) | F-09 |
| loop_003 | タワー 3 種・敵 4 種・相性 | F-04/05 |
| loop_004 | アップグレード / 売却 + 経済仕上げ + 勝敗画面 | F-06/07/08 |
| loop_005 | バランス自動テストハーネス + 数値調整 | F-10 |
| loop_006 | 効果音 + 演出 + ハイスコア | F-08/12 |
| loop_007 | マップ生成器 + マップ選択 | F-11 |
